# values.yaml
replicaCount: 5
image:
 repository: node-hello
 tag: v3
service:
 type: NodePort
 port: 80
ingress:
 enabled: true
 hosts:
   - host: hello-world.local
     paths:
       - path: /
         pathType: Prefix
httpRoute:
 enabled: false